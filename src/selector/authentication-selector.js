export const isAuthenticationLoading = (state) => state.authentication.isLoading
export const isAuthenticationLoaded = (state) => state.authentication.isLoaded
export const walletAddress = (state) => state.authentication.walletAddress
export const chainId = (state) => state.authentication.chainId
export const authError = (state) => state.authentication.error
