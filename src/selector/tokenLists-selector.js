export const isTokenListLoading = (state) => state.supportToken.isLoading
export const isTokenListLoaded = (state) => state.supportToken.isLoaded

export const tokenLists = (state) => state.supportToken.tokenlists
